# --- 코드 품질 ---
ruff>=0.4.0               # 린트 + 자동 포맷
black>=24.4.0             # 포맷터 (ruff 대신 black만 쓰려면 선택)
mypy>=1.10.0              # 정적 타입 체크

# --- 테스트 ---
pytest>=8.2.0
pytest-cov>=5.0.0

# --- Git 훅 ---
pre-commit>=3.7.0
